# =============================================================================
# SENTRY ERROR TRACKING & PERFORMANCE MONITORING
# Travel.AILYDIAN Platform
# =============================================================================

# Sentry DSN (Data Source Name)
# Get this from: https://sentry.io -> Settings -> Projects -> [Your Project] -> Client Keys (DSN)
# Format: https://[public_key]@[org].ingest.sentry.io/[project_id]
NEXT_PUBLIC_SENTRY_DSN=https://your_public_key@your_org.ingest.sentry.io/your_project_id

# Sentry Organization Slug
# Get this from: https://sentry.io -> Settings -> General
SENTRY_ORG=ailydian

# Sentry Project Slug
# Get this from: https://sentry.io -> Settings -> Projects
SENTRY_PROJECT=travel-lydian

# Sentry Auth Token (for source maps upload)
# Generate from: https://sentry.io -> Settings -> Auth Tokens
# Required scopes: project:read, project:write, project:releases
SENTRY_AUTH_TOKEN=your_auth_token_here

# Enable Sentry in Development (optional)
# Set to 'true' to enable Sentry tracking in development
# Default: false (only enabled in production)
NEXT_PUBLIC_SENTRY_ENABLED=false

# App Version (for release tracking)
# Used to track which version of the app errors occurred in
NEXT_PUBLIC_APP_VERSION=2.0.0

# Vercel Git Commit SHA (auto-populated by Vercel)
# Used for release tracking and source map correlation
# VERCEL_GIT_COMMIT_SHA=auto_populated_by_vercel
# NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=auto_populated_by_vercel

# =============================================================================
# CONFIGURATION NOTES
# =============================================================================

# Sample Rates (configured in sentry config files):
# - Client (Browser): 10% in production, 100% in development
# - Server (Node.js): 10% in production, 100% in development
# - Edge (Middleware): 5% in production, 100% in development

# Session Replay (configured in client config):
# - 10% of normal sessions
# - 100% of sessions with errors

# Performance Profiling (configured in server config):
# - 10% of transactions in production
# - CPU and memory profiling enabled

# =============================================================================
# PRIVACY & SECURITY
# =============================================================================

# PII Filtering: Automatically removes
# - Email addresses
# - Phone numbers
# - Credit card numbers
# - Turkish ID numbers (TC Kimlik)
# - IP addresses
# - JWT tokens
# - API keys
# - Database connection strings

# Sensitive Headers Removed:
# - Authorization
# - Cookie
# - X-API-Key
# - X-Auth-Token
# - X-CSRF-Token

# =============================================================================
# GETTING STARTED
# =============================================================================

# 1. Create a Sentry account at https://sentry.io
# 2. Create a new project (choose Next.js platform)
# 3. Copy the DSN from project settings
# 4. Generate an auth token with required scopes
# 5. Update the variables above
# 6. Copy this file to .env.local:
#    cp .env.sentry.example .env.local
# 7. Build and deploy:
#    npm run build
# 8. Test the integration:
#    curl -X POST http://localhost:3100/api/test-sentry?type=exception

# =============================================================================
# TESTING
# =============================================================================

# Test endpoints (development only):
# GET  /api/test-sentry          - Health check
# POST /api/test-sentry?type=exception    - Test error capture
# POST /api/test-sentry?type=message      - Test message capture
# POST /api/test-sentry?type=performance  - Test performance monitoring

# =============================================================================
# MONITORING DASHBOARD
# =============================================================================

# View errors:        https://sentry.io/organizations/[org]/issues/
# Performance:        https://sentry.io/organizations/[org]/performance/
# Session Replays:    https://sentry.io/organizations/[org]/replays/
# Releases:           https://sentry.io/organizations/[org]/releases/

# =============================================================================
# SUPPORT
# =============================================================================

# Documentation: See docs/SENTRY_SETUP.md
# Sentry Docs: https://docs.sentry.io/platforms/javascript/guides/nextjs/
# Support: Contact DevOps team
